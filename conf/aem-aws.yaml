aws:
  access_key_id:
  secret_access_key:
  profile: default
  s3_bucket:
aem:
  component:
  id:
  stack_prefix:
recovery:
  retry_counter: 60
  retry_wait_in_seconds: 60
ready:
  retry_counter: 60
  retry_wait_in_seconds: 60
